# base/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

secretGenerator:
  - name: solra-frontend-secret
    envs:
      - /home/ubuntu/env/solra-forntend-secret.env
    type: Opaque